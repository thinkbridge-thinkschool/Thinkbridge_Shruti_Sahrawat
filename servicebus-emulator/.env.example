# Copy to .env, or let start.ps1 generate one with a random value.
# This password is only ever used by the throwaway SQL Server container that
# backs the local emulator. It is not a credential for anything real, and .env
# is gitignored so no value ever reaches the repository.
MSSQL_SA_PASSWORD=replace-me-with-a-strong-local-only-value
